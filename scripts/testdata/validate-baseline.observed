# RECORDED BASELINE for `make validate`. Generated by
# scripts/validate-timed.sh; promote a fresh one with 'make validate-baseline'.
# Checked by scripts/check-validate-baseline.sh, which runs inside validate.
#
# DO NOT HAND-EDIT, and do not treat these numbers as a budget. Nothing
# asserts a duration: this host runs several agents at once, and duration is
# bimodal by construction, so a threshold here would be a flake generator.
# What IS asserted is that the step set below still equals VALIDATE_STEPS,
# that the measurement is dated and in-window, and that no checkout path
# leaked into this public repo. See QUM-1286.
#
# go_cache=cold is REQUIRED, not incidental: a warm run serves ~43 of 44
# packages from the package cache and can report a duration for one of them,
# so a warm per-package baseline is a table of absences. lint_cache is
# reported rather than required — GOLANGCI_LINT_CACHE is per-worktree, so a
# fresh agent worktree pays cold and an established one does not.
#
# Row format: step<TAB>name<TAB>seconds<TAB>rc  and  pkg<TAB>import-path<TAB>seconds.
# A step row's rc must be 0 in a promoted baseline: a failing step exits
# early, so its duration is time-to-failure rather than the cost of the
# work, and that is a wrong number rather than a missing one.
#
# Fields: total_wall_s is measured end to end. driver_overhead_s is this
# script's own cost (probes and bookkeeping) and excludes the two structural
# costs of stepwise timing, which are one duplicate warm 'build' (validate
# runs it as a step and again as hooks-armed's prerequisite) and one make
# parse per step.
recorded=2026-08-25
cores=8
go=go1.26.6
os_arch=linux/arm64
go_cache=cold
lint_cache=warm
total_wall_s=195.94
driver_overhead_s=0.06
step	build	0.37	0
step	test-build-stamp	5.15	0
step	hooks-armed	0.40	0
step	proto-check	0.81	0
step	fmt-check	7.41	0
step	lint	1.89	0
step	test-lint-pin	10.34	0
step	test-race-gate	1.97	0
step	test-race	79.19	0
step	test-doclint	3.56	0
step	test-doclint-split-unit	6.56	0
step	test-check-scope-unit	7.81	0
step	test-check-budget-unit	0.07	0
step	test-precommit-gate-unit	0.18	0
step	test-wirelog-helpers-unit	0.48	0
step	test-e2e-lockwait-unit	15.56	0
step	test-e2e-matrix-unit	32.25	0
step	test-always-loaded-budget-unit	15.14	0
step	always-loaded-budget	0.49	0
step	test-gitignore-classes	2.83	0
step	test-validate-timing-unit	2.18	0
step	check-validate-baseline	0.06	0
step	leak-scan	1.18	0
pkg	github.com/dmotles/sprawl/internal/supervisor	57.108
pkg	github.com/dmotles/sprawl/internal/hub	33.693
pkg	github.com/dmotles/sprawl/internal/runtime	24.010
pkg	github.com/dmotles/sprawl/internal/githooks	16.986
pkg	github.com/dmotles/sprawl/internal/hub/store	15.447
