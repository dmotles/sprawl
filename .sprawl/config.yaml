validate: make validate
worktree.setup: 'if [ -f "$SPRAWL_ROOT/CLAUDE.local.md" ]; then cp "$SPRAWL_ROOT/CLAUDE.local.md" "$PWD/"; else echo "FYI: no CLAUDE.local.md found in $SPRAWL_ROOT — agents may lack workspace-specific config" >&2; fi'
worktree.teardown: ""
