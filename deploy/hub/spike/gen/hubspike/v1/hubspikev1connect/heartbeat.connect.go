// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: hubspike/v1/heartbeat.proto

// QUM-871 transport spike. Throwaway service used ONLY to prove a heartbeated
// Connect server-stream survives a managed L7 (Envoy) ingress idle timeout and
// that from_seq reconnect resumes with zero gaps/dupes. Not part of the product.
package hubspikev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/dmotles/sprawl/hubspike/gen/hubspike/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// HeartbeatServiceName is the fully-qualified name of the HeartbeatService service.
	HeartbeatServiceName = "hubspike.v1.HeartbeatService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// HeartbeatServiceSubscribeProcedure is the fully-qualified name of the HeartbeatService's
	// Subscribe RPC.
	HeartbeatServiceSubscribeProcedure = "/hubspike.v1.HeartbeatService/Subscribe"
)

// HeartbeatServiceClient is a client for the hubspike.v1.HeartbeatService service.
type HeartbeatServiceClient interface {
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.Frame], error)
}

// NewHeartbeatServiceClient constructs a client for the hubspike.v1.HeartbeatService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewHeartbeatServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) HeartbeatServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	heartbeatServiceMethods := v1.File_hubspike_v1_heartbeat_proto.Services().ByName("HeartbeatService").Methods()
	return &heartbeatServiceClient{
		subscribe: connect.NewClient[v1.SubscribeRequest, v1.Frame](
			httpClient,
			baseURL+HeartbeatServiceSubscribeProcedure,
			connect.WithSchema(heartbeatServiceMethods.ByName("Subscribe")),
			connect.WithClientOptions(opts...),
		),
	}
}

// heartbeatServiceClient implements HeartbeatServiceClient.
type heartbeatServiceClient struct {
	subscribe *connect.Client[v1.SubscribeRequest, v1.Frame]
}

// Subscribe calls hubspike.v1.HeartbeatService.Subscribe.
func (c *heartbeatServiceClient) Subscribe(ctx context.Context, req *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.Frame], error) {
	return c.subscribe.CallServerStream(ctx, req)
}

// HeartbeatServiceHandler is an implementation of the hubspike.v1.HeartbeatService service.
type HeartbeatServiceHandler interface {
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.Frame]) error
}

// NewHeartbeatServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewHeartbeatServiceHandler(svc HeartbeatServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	heartbeatServiceMethods := v1.File_hubspike_v1_heartbeat_proto.Services().ByName("HeartbeatService").Methods()
	heartbeatServiceSubscribeHandler := connect.NewServerStreamHandler(
		HeartbeatServiceSubscribeProcedure,
		svc.Subscribe,
		connect.WithSchema(heartbeatServiceMethods.ByName("Subscribe")),
		connect.WithHandlerOptions(opts...),
	)
	return "/hubspike.v1.HeartbeatService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case HeartbeatServiceSubscribeProcedure:
			heartbeatServiceSubscribeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedHeartbeatServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedHeartbeatServiceHandler struct{}

func (UnimplementedHeartbeatServiceHandler) Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.Frame]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("hubspike.v1.HeartbeatService.Subscribe is not implemented"))
}
