{
  "name": "agent_retired",
  "version": 1,
  "opens": false,
  "spillable": true,
  "closes": "agent_spawned",
  "json_schema": {
    "type": "object",
    "description": "Closes the agent_spawned contract.",
    "required": ["agent_name", "outcome"],
    "properties": {
      "agent_name": {"type": "string"},
      "outcome": {"type": "string"},
      "merged": {"type": "boolean"},
      "legacy": {"type": "boolean"}
    }
  }
}
