{
  "name": "repo_initialized",
  "version": 1,
  "opens": false,
  "spillable": true,
  "closes": "",
  "json_schema": {
    "type": "object",
    "description": "First time the store is enabled for a project.",
    "required": ["git_sha", "remote_url"],
    "properties": {
      "git_sha": {"type": "string"},
      "remote_url": {"type": "string"},
      "host": {"type": "string"}
    }
  }
}
