{
  "name": "notify_acked",
  "version": 1,
  "opens": false,
  "spillable": false,
  "closes": "owner_notify",
  "json_schema": {
    "type": "object",
    "description": "Closes an owner_notify. The plan says this is closed 'by the runtime at the recipient's turn boundary'; in M1b it is closed by a cursor consumer that observes the recipient's next turn_finished event, which reaches the same conclusion from the log rather than through a runtime hook — the local AgentState taxonomy stays the sole wake arbiter and internal/supervisor is untouched. NOT spillable (contract event).",
    "required": ["recipient"],
    "properties": {
      "recipient": {"type": "string"},
      "turn_event_id": {"type": "string"},
      "host": {"type": "string"}
    }
  }
}
