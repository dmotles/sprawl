{
  "name": "user_answered",
  "version": 1,
  "opens": false,
  "spillable": false,
  "closes": "user_question",
  "json_schema": {
    "type": "object",
    "description": "Closes a user_question. answered_by records WHICH human, because a multi-operator fleet in which every answer is attributed to 'the user' cannot be audited. NOT spillable (contract event).",
    "required": ["answer"],
    "properties": {
      "answer": {"type": "string"},
      "answered_by": {"type": "string"},
      "host": {"type": "string"}
    }
  }
}
